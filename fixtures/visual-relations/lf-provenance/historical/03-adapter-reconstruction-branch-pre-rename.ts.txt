  if (reconstruction) {
    const pronounced = flattenAnchorIds(reconstruction.anchors.pronouncedCopy)[0];
    const lower = flattenAnchorIds(reconstruction.anchors.reconstructedCopy)[0];
    lens.lf = {
      ...(fallbackLens?.lf || {}),
      strikeNodes: [lower],
      ghostNodes: collectSubtreeIds(nodes.get(lower)),
      paths: [{ from: lower, to: pronounced, kind: 'qr', elbow: false, arrow: false }]
    };
  }
