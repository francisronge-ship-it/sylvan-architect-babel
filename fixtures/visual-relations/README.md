# Visual Relation Proof Fixtures

These compact files are the exact historical evidence consumed by
`tests/visualRelationsLabAdapter.test.mjs`. They were promoted from ignored
`.artifacts/` directories so the default provider-free suite works in a fresh
checkout.

The fixtures are immutable characterization evidence. New browser captures
belong in `.artifacts/` until a focused test proves that a compact subset must
be committed.
